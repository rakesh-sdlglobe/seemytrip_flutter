import 'package:flutter/material.dart';

class AppDimens {
  // Spacing
  static const double spaceXXS = 2.0;
  static const double spaceXS = 4.0;
  static const double spaceS = 8.0;
  static const double spaceM = 16.0;
  static const double spaceL = 24.0;
  static const double spaceXL = 32.0;
  static const double spaceXXL = 48.0;

  // Border Radius
  static const double radiusXS = 4.0;
  static const double radiusS = 8.0;
  static const double radiusM = 12.0;
  static const double radiusL = 16.0;
  static const double radiusXL = 24.0;
  static const double radiusRound = 100.0;

  // Elevation
  static const double elevationNone = 0.0;
  static const double elevationXS = 1.0;
  static const double elevationS = 2.0;
  static const double elevationM = 4.0;
  static const double elevationL = 8.0;
  static const double elevationXL = 16.0;

  // Icon Sizes
  static const double iconSizeXS = 16.0;
  static const double iconSizeS = 20.0;
  static const double iconSizeM = 24.0;
  static const double iconSizeL = 32.0;
  static const double iconSizeXL = 48.0;

  // Button Sizes
  static const double buttonHeight = 48.0;
  static const double buttonHeightSmall = 36.0;
  static const double buttonHeightLarge = 56.0;
  static const double buttonMinWidth = 88.0;

  // Input Fields
  static const double inputFieldHeight = 48.0;
  static const double inputFieldBorderWidth = 1.0;
  static const double inputFieldFocusedBorderWidth = 2.0;

  // App Bar
  static const double appBarHeight = 56.0;
  static const double appBarElevation = 0.0;

  // Bottom Navigation Bar
  static const double bottomNavBarHeight = 56.0;
  static const double bottomNavBarElevation = 8.0;

  // Dialog
  static const double dialogBorderRadius = 12.0;
  static const double dialogElevation = 24.0;

  // Card
  static const double cardElevation = 2.0;
  static const double cardBorderRadius = 12.0;

  // Divider
  static const double dividerThickness = 1.0;
  static const double dividerIndent = 0.0;
  static const double dividerEndIndent = 0.0;

  // Padding
  static const EdgeInsets screenPadding = EdgeInsets.all(spaceM);
  static const EdgeInsets horizontalPadding = EdgeInsets.symmetric(horizontal: spaceM);
  static const EdgeInsets verticalPadding = EdgeInsets.symmetric(vertical: spaceM);
  static const EdgeInsets allPaddingXS = EdgeInsets.all(spaceXS);
  static const EdgeInsets allPaddingS = EdgeInsets.all(spaceS);
  static const EdgeInsets allPaddingM = EdgeInsets.all(spaceM);
  static const EdgeInsets allPaddingL = EdgeInsets.all(spaceL);

  // Margin
  static const EdgeInsets marginXS = EdgeInsets.all(spaceXS);
  static const EdgeInsets marginS = EdgeInsets.all(spaceS);
  static const EdgeInsets marginM = EdgeInsets.all(spaceM);
  static const EdgeInsets marginL = EdgeInsets.all(spaceL);
  static const EdgeInsets marginXL = EdgeInsets.all(spaceXL);

  // Animation Durations
  static const Duration animationShort = Duration(milliseconds: 200);
  static const Duration animationMedium = Duration(milliseconds: 350);
  static const Duration animationLong = Duration(milliseconds: 500);
}
